A quick (and possibly dirty) module to try out the use #attached in the Drupal 7
form API.

The module creates a form with a text field and attaches a JS file and some
settings to the form's render array. The JS displays the settings in the
browser's console.